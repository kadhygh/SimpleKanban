# v1 交接文档

最后更新：2026-03-12（v1 准备结束，转入 v2 规划）

## 0. 阶段说明

- 本文档保留 `v1` 最后一轮实现阶段的事实快照与交接口径。
- `v1` 当前实现阶段已完成验证与收口准备，可视为“准备结束”的历史阶段。
- 自 2026-03-12 起，后续活跃规划不再继续堆叠在 `v1` 文档中，而是转入：
  - `docs/推进/v2/README.md`
  - `docs/推进/v2/修订版里程碑草案.md`
- 下文中关于 `M7.3` 的内容，保留为 `v1` 收束前的最后计划口径，用于理解代码和阶段演进，不再作为唯一的后续事实来源。

## 1. 当前结论（已由你本人验证通过）

已完成并通过人工验证的当前节点：

- `M1`：本地服务 + 项目选择 + 持久化
- `M2`：网页终端（xterm.js + WebSocket + node-pty）最小闭环 + 页面刷新恢复
- `M4`：执行器模型（列表/预览/注入，单参数输入）
- `M5`：TaskCard 执行闭环（Run -> Terminal -> 状态回流）
- `M3`：会话可信性当前节点（服务重启：残留 `running/waiting` -> `lost`，避免“假运行”）
- `M6`：结构层当前节点（Task `dependencyIds` + 结构视图展示 + focus-only 子图）

当前仍保持的范围约束：

- 单机 / 单项目 / 单活跃终端（所有 TaskCard 共享一个活跃终端会话）
- 依赖关系仅用于结构展示，不做运行约束（但实现上保留未来加 gating 的扩展点）
- 产物层暂不做（后续有明确需求再加）
- 记忆层（memory）暂时封存（默认禁用）

下一最小节点（已拍板）：

- `M7.3`
- 目标是决定“结构视图”是继续作为独立区块保留，还是逐步被 canvas 吸收
- 当前只做信息架构与交互收口，不做完整白板编辑器，也不做大规模页面重构

当前代码实情已经前进到这一步：

- `M7 Canvas Spike` 第一刀已经提交
- `M7.2` 工作台 <-> canvas 双向定位已由你本人验证通过，并已提交为 `affe77a`

## 2. 快速启动

如是首次拉起，先执行：

```bash
npm install
```

然后在仓库根目录运行：

```bash
npm run dev
```

然后打开：

```text
http://127.0.0.1:3210
```

如果 `3210` 被占用（`EADDRINUSE`），可以改端口：

```powershell
$env:PORT=3211; npm run dev
```

## 3. 本地持久化（状态文件）

- 位置：`source/server/data/project-state.json`（已在 `.gitignore` 中忽略）
- 内容：`project`、`tasks`（以及启用 memory 时的 `memory`）

重置方式：

1. 停止服务。
2. 删除 `source/server/data/project-state.json`（或清空文件内容）。
3. 重新执行 `npm run dev`。

## 4. 环境变量

- `HOST`：监听地址，默认 `127.0.0.1`
- `PORT`：监听端口，默认 `3210`
- `PTY_SHELL`：指定 PTY 启动 shell
- `SIMPLEKANBAN_ENABLE_MEMORY`：`1` 表示启用 memory（当前默认封存且未接 UI）

补充说明：

- Windows 默认 PTY shell 是 `powershell.exe -NoLogo`；
- 非 Windows 默认使用 `$SHELL`，否则回退到 `bash`；
- v1 当前没有把 memory 暴露为 API，也没有把它接入 UI。

## 5. 关键数据结构（当前实现口径）

### 5.1 Project

```json
{
  "name": "SimpleKanban",
  "path": "D:\\Projects\\SimpleKanban",
  "updatedAt": "2026-03-11T00:00:00.000Z"
}
```

### 5.2 Task

字段（主要）：

- `id`, `title`, `description`, `executorId`
- `status`: `idle | running | waiting | lost | ended`
- `dependencyIds`: `string[]`
- `linkedSessionId`: `string | null`
- `lastRunAt`, `lastStatusAt`, `lastTerminalActivityAt`, `lastTerminalOutput`
- `attachments`: 目前支持 `note`（备注）类型

状态语义：

- `idle`：未运行
- `running`：已触发运行（命令已写入共享终端）
- `waiting`：检测到需要人工输入/确认（启发式基于终端输出关键字）
- `ended`：终端会话已关闭/结束
- `lost`：服务重启后无法确认之前运行的终端会话，标记为丢失，需要手动重新运行

### 5.3 TerminalSession

`GET /api/terminal/session` 返回的会话快照包含（主要字段）：

- `id`, `projectPath`, `cwd`, `shell`, `pid`, `cols`, `rows`
- `status`: `starting | running | closed | error`
- `createdAt`, `startedAt`, `endedAt`, `lastOutputAt`, `exitCode`, `error`

## 6. API 概览

- `GET /api/health`
- `GET /api/project/current`
- `POST /api/project/select`：空 body 会弹 Windows 目录选择器；也支持传 `{ "path": "D:\\\\Projects\\\\X" }`
- `GET /api/executors`
- `POST /api/executors/preview`
- `POST /api/executors/inject`（或 `POST /api/executors/run`）
- `GET /api/terminal/session`
- `POST /api/terminal/close`
- `GET /api/tasks`
- `POST /api/tasks`
- `POST /api/tasks/run`
- `POST /api/tasks/dependencies`
- `POST /api/tasks/note`

## 7. 关键代码位置

服务端：

- `source/server/server.mjs`：HTTP API + WS + Task 运行与状态回流
- `source/server/lib/project-store.mjs`：Project/Task 持久化、M3 reconcile、（封存的）memory 结构
- `source/server/lib/terminal-session-manager.mjs`：单活跃终端会话（node-pty）
- `source/server/lib/executor-profiles.mjs`：执行器注册表、参数规范化与命令拼装
- `source/server/lib/folder-dialog.mjs`：Windows 目录选择器

前端：

- `source/web/index.html`
- `source/web/app.js`
- `source/web/styles.css`
- `source/web/canvas.html`
- `source/web/canvas.css`
- `source/web/canvas.js`

## 8. 验证闸门规则（流程约定）

- 每完成一个最小步骤先暂停，输出“待你本人验证项（操作步骤 + 预期结果）”。
- 只有你明确回复“验证通过”，才继续下一个步骤。
- 未经你本人验证的能力，只能写成“已实现 / 待验证”，不能直接写成“已完成验收”。

## 9. 当前待推进内容（换设备后优先看这里）

当前最近已提交内容：

- `source/web/index.html`：`Canvas` 导航入口支持根据当前焦点任务动态带参跳转
- `source/web/app.js`：工作台读取 `?task=`、TaskCard/结构焦点同步、回跳高亮、“在 Canvas 中定位”按钮
- `source/web/styles.css`：TaskCard 选中态与回跳高亮样式
- `source/web/canvas.html`：返回工作台链接落到任务区并保留任务定位
- `source/web/canvas.js`：canvas 读取/回写 `?task=`，并把当前选中任务带回工作台

当前节点口径：

- `M7 Canvas Spike` 第一刀已经提交
- `M7.2` 已完成：canvas 选中态和工作台任务区形成更明确的双向联动
- `M7.2` 已由你本人验证通过，并已提交为 `affe77a`
- 当前下一最小节点切换为 `M7.3`

`M7.3` 的工作内容应收敛在这四件事：

1. 决定结构视图与 canvas 的主从关系：
   - 是保留“结构视图 + canvas”双区并存；
   - 还是让 canvas 成为主结构界面，结构视图退化为辅助入口或局部摘要。
2. 收口“焦点任务 / 子图 / 相邻关系”的唯一表达：
   - 当前结构视图已有 focus-only 子图；
   - `M7.3` 需要判断这套语义是否迁到 canvas，避免两套结构表达长期并行。
3. 决定哪些信息继续留在 TaskCard，哪些开始靠近 canvas：
   - 依赖关系概览；
   - 被依赖关系概览；
   - note / 说明；
   - 终端入口与运行入口。
4. 做一个最小页面收口版本，用来验证主界面方向：
   - 可以是“保留结构视图，但默认弱化”；
   - 也可以是“canvas 成主视图，结构视图只保留编辑/诊断入口”；
   - 但必须仍然可由你本人在一轮里清楚验证。

`M7.3` 当前明确不做：

- 不做自由拖拽编排
- 不做缩放/平移手势系统重写
- 不做完整连线编辑
- 不做运行约束 gating
- 不做产物层与 memory 恢复
- 不做整站视觉大翻修

推荐的 `M7.3` 最小落地顺序：

1. 先拍板“结构视图是保留还是弱化”的方向
2. 再把 focus-only 子图 / 焦点联动统一到一个主要界面
3. 然后决定依赖概览与详情信息落在 canvas 侧还是 TaskCard 侧
4. 最后只做一轮足够验证方向的页面收口，不提前扩自由编辑能力

## 10. 后续推进建议

当前推荐按这个顺序继续：

1. `M7.3`：决定“结构视图”是继续保留，还是逐步被 canvas 吸收
2. `M7.4`：决定 note/说明/依赖/终端入口，哪些挂在节点附近，哪些放到详情区
3. 验证完主界面方向后，再考虑一轮页面表现重构

当前明确不建议提前做：

- 完整无限画布编辑器
- 复杂拖拽编排
- 复杂缩放/平移手势系统
- 完整连线编辑
- 产物层
- memory 恢复

## 11. 当前提交参考

最近几个关键提交：

- `affe77a` - `feat: add workspace-canvas task deep linking`
- `abb8c53` - `feat 提交交接信息，和相关脚本呢`
- `1861e78` - `docs: plan canvas spike`
- `983386e` - `feat: add workspace overview summary`
- `b1ddceb` - `feat: tidy workspace shell layout`
- `41e8205` - `docs: update v1 progress and handoff`

## 12. 下一步建议（口径）

直接进入 `M7.3`：

1. 继续保持独立 canvas 页面，不急着收成完整白板；
2. 围绕“结构视图是否还需要作为独立区块存在”做最小决策；
3. 优先统一焦点任务 / 子图 / 依赖关系的主要表达面；
4. 验证通过后，再进入 `M7.4` 的信息落点收口。
