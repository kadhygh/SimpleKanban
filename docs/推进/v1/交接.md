# v1 交接文档

最后更新：2026-03-07

## 1. 当前结论

当前已经完成 `M1：本地服务 + 项目选择` 的最小可运行实现。

同时，`M2：网页终端最小闭环` 的核心链路已完成首轮人工验证：终端可创建、可输入输出、`resize` 复验通过。

当前仍未进入“整个 v1 已完成”的口径，后续若继续补会话恢复等能力，仍需按“先实现一个最小节点，再由你本人验证”的规则继续推进。

这次落地没有直接上 `React + xterm.js + node-pty`，而是先用**零依赖 Node 本地服务 + 原生 HTML/CSS/JS 页面**把第一条链路打通，确保后续可以在这个基础上继续进入 `M2`。

当前已经跑通的能力：

- 本地服务启动
- 网页通过同一服务访问
- 读取当前项目
- 选择项目目录
- 本地持久化当前项目路径
- 页面展示当前项目路径和服务状态

当前正在接入的能力：

- 网页终端展示区域
- `xterm.js` 终端前端接线
- 后端单活跃终端会话模型
- `WebSocket + node-pty` 的双向终端通信
- 终端 `resize` 与最小会话状态同步

说明：

- 上述内容表示“已开始实现 / 正在接线”，并不自动代表“已完成可验证验收”。
- 具体哪些子项已经真正跑通，应以当前仓库代码和最新验证结果为准。

## 2. 本次新增 / 变更文件

### 根目录
- `package.json`
- `.gitignore`
- `README.md`

### 文档
- `docs/dashboard.md`
- `docs/推进/v1/清单.md`
- `docs/推进/v1/交接.md`

### M1 代码
- `source/server/server.mjs`
- `source/server/lib/project-store.mjs`
- `source/server/lib/folder-dialog.mjs`
- `source/web/index.html`
- `source/web/app.js`
- `source/web/styles.css`

## 3. 当前代码结构说明

M1 已落地的主文件仍然是当前实现入口；M2 在此基础上继续演进时，建议优先查看现有代码与新增 terminal 相关文件，而不是假设项目已经整体切换技术栈。

### `source/server/server.mjs`
职责：

- 提供本地 HTTP 服务
- 提供 API：
  - `GET /api/health`
  - `GET /api/project/current`
  - `POST /api/project/select`
- 提供静态页面服务，直接把 `source/web` 作为前端资源目录

### `source/server/lib/project-store.mjs`
职责：

- 维护当前项目状态的本地读写
- 当前保存位置：`source/server/data/project-state.json`
- 该目录已加入 `.gitignore`

### `source/server/lib/folder-dialog.mjs`
职责：

- 调用 Windows PowerShell 的 `System.Windows.Forms.FolderBrowserDialog`
- 让后端弹出原生文件夹选择窗口

注意：

- 这部分是 **Windows-only** 的本地实现
- 当前完全符合项目目前的本地 Unity 开发场景

### `source/web/index.html`
职责：

- M1 页面骨架
- 展示当前项目、服务状态和状态输出区

### `source/web/app.js`
职责：

- 调用健康检查接口
- 拉取当前项目
- 触发“选择工程”按钮
- 更新页面状态

### `source/web/styles.css`
职责：

- 提供当前 M1 页面样式

## 3.1 M2 当前实现路线

当前 M2 的实施路线已经收敛为：

- 保留当前本地 Node 服务，不额外引入完整后端框架；
- 保留当前原生 HTML/CSS/JS 页面，不在本轮强行切换到 React/Vite；
- 在现有页面中增加终端区域与连接状态展示；
- 在现有服务中增加单项目、单活跃终端会话能力；
- 先打通 `xterm.js <-> WebSocket <-> node-pty` 最小交互链路；
- 再基于实际稳定性决定后续技术栈演进。

## 4. 当前 API 说明

### `GET /api/health`
返回服务健康状态。

示例响应：

```json
{
  "ok": true,
  "app": "SimpleKanban",
  "version": "0.1.0-m1"
}
```

### `GET /api/project/current`
返回当前保存的项目路径。

示例响应：

```json
{
  "project": {
    "name": "SimpleKanban",
    "path": "D:\\Projects\\SimpleKanban",
    "updatedAt": "2026-03-07T01:00:53.224Z"
  }
}
```

### `POST /api/project/select`
用途有两种：

#### 模式 A：网页正常调用
请求体为空对象 `{}` 时：

- 服务端弹出原生文件夹选择器
- 用户选择后保存项目路径

#### 模式 B：脚本 / 测试调用
请求体可传：

```json
{
  "path": "D:\\Projects\\SimpleKanban"
}
```

这样可以跳过原生对话框，直接设置项目路径。

这个能力是为了：

- 自动化测试方便
- 调试方便
- 后续接口复用更方便

## 5. 当前运行方式

在项目根目录执行：

```bash
npm run dev
```

然后打开：

```text
http://127.0.0.1:3210
```

## 5.1 用户验证优先规则

从 2026-03-07 开始，本项目后续推进采用“**用户本人验证闸门**”规则：

- 开发侧自测只能作为参考，不等同于最终验收。
- 里程碑是否算“完成”，以你本人实际操作验证结果为准。
- 每完成一个最小步骤，都要先停下，给出你需要执行的验证操作和预期结果。
- 只有在你明确回复“验证通过”后，才继续推进下一步。
- 未经你本人验证的能力，应写成“已实现 / 待验证”，不能直接写成“已完成”。

当前验证结论更新：

- `M1` 已由你本人验证通过；
- 当前后续验证闸门已经推进到 `M2`。

## 6. 开发侧验证记录（不等同用户验收）

本轮已经做过的验证：

- 直接运行 `node source/server/server.mjs` 成功启动服务
- `GET /api/health` 返回正常
- `GET /api/project/current` 可读取空状态和已保存状态
- `POST /api/project/select` 通过传 `path` 的方式验证保存流程成功
- `/` 页面可以正常返回 `200`

说明：

- `npm run dev` 的脚本本质上也是启动同一个 `node source/server/server.mjs`
- 已验证核心启动链路有效
- 但以上仅代表开发侧验证记录，当前仍需要你本人补做一次实际操作确认

## 7. 当前设计取舍

### 为什么 M1 没直接上 React / Vite
因为当前阶段最重要的是：

- 尽快打通本地服务
- 确认项目选择链路可行
- 确认前后端连接方式可行
- 为 M2 的 PTY / 网页终端留出清晰落点

如果一开始就上完整前端栈，会增加很多与当前目标无关的初始化成本。

### 为什么项目选择放后端
原因：

- 纯浏览器不适合稳定获取本地绝对路径
- 当前能力本质上是本地系统能力，不是 Web 沙盒能力
- 后端弹原生目录选择器更符合后续“本地工具”定位

### 为什么保留 `path` 直传模式
因为它能明显降低调试和测试成本，也方便后续 TaskCard 或执行器模型复用这条接口。

## 8. 当前未做的内容

以下仍未完成或尚未进入稳定验收：

- `TaskCard` 绑定执行器
- 脑图 / 看板 / 结构层数据模型
- 长期上下文的图形化承载
- TaskCard 绑定执行器

也就是说：

**当前 M1 已完成；M2 的最小网页终端闭环与最小执行器链路也已经通过人工验证。下一阶段更适合把重点逐步转向结构层与记忆层。**

## 9. 下一步建议

优先级调整如下：

1. 新对话先阅读 `docs/项目定位.md`，确认项目已经从“终端托管”提升为“结构化 AI 工作台”的方向。
2. 若继续做产品设计，优先进入“结构层 / 记忆层”文档：先明确脑图、Task、TerminalSession、Executor 之间的关系。
3. 若继续做代码实现，也应直接服务结构化工作台目标，而不是只继续堆终端细节。
4. 继续保持“每完成一个最小节点就先停下、等你验证”的节奏。

## 10. M1 用户验收记录

你本人已确认 `M1` 验证通过。

本次确认通过的点包括：

- 服务可以正常启动；
- 页面可以正常打开；
- 当前项目信息可以读取；
- “选择工程”可以正常选择目录并刷新显示；
- 刷新页面后，项目路径仍然保留。

因此，`M1` 当前口径可以正式更新为“已由你本人验收通过”。

## 11. M2 当前人工验证记录

你本人已完成当前 `M2` 最小节点验证，确认结果如下：

- 网页终端可以正常出现；
- 可以输入命令并正确输出结果；
- 终端 `resize` 在修正后复验通过；
- 页面刷新后可以自动恢复当前运行中的终端连接，并继续输入输出；
- 执行器注入已经通过人工验证，可以把预设命令注入当前终端并生成测试产物；
- 执行器参数输入已经通过人工验证：文件名变化会实时反映到命令预览，不输入时会安全回退到默认文件名；
- 执行器状态栏已经通过人工验证：状态会随上下文正确变化，并能稳定反馈“可注入 / 注入中 / 已注入 / 缺少前置条件”；
- 服务关闭后会话状态显示 `closed`，这一表现符合预期。

当前结论：

- `M2` 最小网页终端闭环已经通过当前轮次人工验证；
- 执行器模型已经完成一个最小可用样例；
- 执行器模型已经从固定模板升级为单参数可配置样例；
- 执行器状态反馈也已达到当前阶段可用水平；
- 当前若继续推进，更适合从“结构层 / 记忆层”切入，而不是回头重复最小终端链路。

建议新对话接手后，直接把当前项目视为：

- `M1` 已验收通过；
- `M2` 当前节点已验收通过；
- 最小执行器链路已验收通过；
- 下一阶段要重点解决的是“人如何快速看懂当前结构，并在中断后快速接上”。

推荐推进顺序：

1. 先阅读 `docs/项目定位.md`
2. 核对 `docs/dashboard.md`、`docs/推进/v1/清单.md` 与当前代码状态是否一致
3. 设计下一阶段的最小结构层文档或数据模型
4. 继续按“实现一个最小节点 -> 你本人验证 -> 再继续”的节奏推进

## 10. M2 开始前建议保留的约束

- 继续保持 **单机 / 单项目 / 单活跃终端** 的最小范围
- 不要提前引入 TaskCard 业务逻辑
- 不要提前引入账号 / 协作 / 远程服务
- 不要提前引入 worktree
- 外部 terminal 继续保持为后续扩展，不作为当前主路径

## 11. 与参考资料的关系

`docs/rescource` 中已有两份参考资料：

- `docs/rescource/vibe-kanban-cli-analysis.md`
- `docs/rescource/vibekanban项目分析.md`

它们可以作为：

- CLI 集成方式参考
- WebSocket 消息流参考
- xterm.js 交互模型参考

但不是必须照搬的实现模板。

如果有更简单、更适合当前项目的方式，应优先选择当前项目自己的实现路线。

## 12. 下一次对话可以直接这样接

可以直接用类似下面的话继续：

> 继续推进 SimpleKanban。请先阅读 `docs/项目定位.md`、`docs/推进/v1/交接.md`、`docs/推进/v1/清单.md`、`prepare.md`、`docs/dashboard.md`。当前网页终端与最小执行器链路都已通过人工验证，接下来请优先推进“结构层 / 记忆层”的下一最小节点，并继续保持“每完成一步先停下、等我本人验证通过后再继续”的节奏。

