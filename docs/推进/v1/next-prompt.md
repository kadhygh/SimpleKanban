# 下一次对话提示词

这个文件用于在开启新对话时，快速把上下文交给新的助手实例。

## 推荐提示词

```text
继续推进 SimpleKanban。

请先阅读以下文件，再继续实现：
1. `docs/项目定位.md`
2. `docs/推进/v1/交接.md`
3. `docs/推进/v1/清单.md`
4. `prepare.md`
5. `docs/dashboard.md`

当前状态：
- M1 已完成，并已由用户本人验证通过。
- M2 当前节点已完成，并已由用户本人验证通过。
- M4（执行器模型）当前节点已完成，并已由用户本人验证通过。
- M5（TaskCard 执行闭环）当前节点已完成，并已由用户本人验证通过。
- M3（会话可信性）当前节点已完成：服务重启后残留 `running/waiting` 任务会被标记为 `lost`，避免“假运行”。
- 结构层当前节点已完成：Task 支持 `dependencyIds`，结构视图可选中高亮、focus-only 子图，并提示环依赖。
- `M7.1` 已完成并经用户本人验证：独立 canvas 页面、节点投影、依赖连线、节点选中与详情联动已打通。
- `M7.2` 已完成并经用户本人验证，且已提交为 `affe77a`：工作台与 canvas 已支持基于 `?task=` 的双向定位和回跳高亮。
- 下一最小节点已确定为 `M7.3`：重点不再是“有没有 canvas”，而是判断结构视图是否继续保留，还是开始逐步被 canvas 吸收。
- 记忆层（memory）当前封存；产物层当前不考虑（后续有需求再补）。
- 用户这几天的真实体感是：CLI 执行很强，但长期上下文与整体结构更需要图形化表达，因此 `docs/项目定位.md` 非常关键。

当前代码位置：
- `source/server/server.mjs`
- `source/server/lib/project-store.mjs`
- `source/server/lib/terminal-session-manager.mjs`
- `source/server/lib/executor-profiles.mjs`
- `source/server/lib/folder-dialog.mjs`
- `source/web/index.html`
- `source/web/app.js`
- `source/web/styles.css`
- `source/web/canvas.html`
- `source/web/canvas.css`
- `source/web/canvas.js`

如果仓库里已经出现 terminal / executor 相关新增文件，也请一并阅读，并以当前代码实情为准。

接下来请优先推进 `M7.3`，并优先解决“结构视图与 canvas 的关系收口”这个问题，而不是继续泛化地做 canvas spike。

建议的新阶段目标：
- 明确结构视图在当前主界面里是“辅助入口”还是“逐步退出主舞台”
- 明确 `selected task / focus-only subgraph / dependency relation` 在工作台与 canvas 之间的唯一语义
- 明确依赖编辑、note、运行、查看终端等动作，哪些继续留在 TaskCard，哪些适合迁到 canvas 周边或详情区
- 选择一个可回退、可由用户本人快速验证的最小 `M7.3` 切口
- 继续保持每一步都可由用户本人验证

实现约束：
- 继续保持单机、单项目、单活跃终端
- 依赖关系当前只做结构展示，不做运行约束（保留未来加 gating 的扩展点）
- `M7.3` 仍然只验证主界面方向，不要直接扩成完整白板编辑器
- 不要在这一轮直接引入复杂拖拽、自由编排、复杂缩放/平移或完整连线编辑
- 不要为了做 `M7.3` 一次性删除现有结构视图，优先选择可逆的小步收口
- 不要提前引入账号、多人协作、远程服务、worktree
- 外部 terminal 不是当前主路径，优先网页托管终端
- `docs/rescource` 里的 vibekanban 分析可以参考，但不必照搬
- 当前阶段不要为了结构层设计而一口气引入过大框架，仍然优先做最小闭环

强制流程：
- 每完成一个步骤先暂停，输出“待我本人验证项（操作步骤 + 预期结果）”。
- 只有我明确回复“验证通过”，你才继续下一个步骤。
- 不要把开发自测结果当成我的验收结果。

请先给出一个简短计划，说明：
1. 当前哪些能力已由用户本人验证通过；
2. `M7.2` 实际验证结果说明了什么，以及为什么现在该进入 `M7.3`；
3. 你准备选择哪个最小 `M7.3` 切口，以及它如何服务“结构化 AI 工作台”而不只是继续堆终端细节；
然后再继续实现。
```

## 使用建议

- 如果下一次对话的目标仍然是继续开发，直接复制上面的提示词即可。
- 如果下一次对话只想讨论方案，也建议先让助手阅读 `docs/推进/v1/交接.md`。
- 如果后续进入新阶段（例如产物层、记忆层、页面重构），可以在这个文件基础上继续更新提示词。
