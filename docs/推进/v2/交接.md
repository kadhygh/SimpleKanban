# v2 交接文档

最后更新：2026-03-19

## 0. 当前阶段

- `M7.3` 已完成当前节点，并已由你本人验证通过。
- `M8.1` 已完成当前节点，并已由你本人验证通过。
- 上一轮 `M8.1` 收口验证已经通过，当前不再以“先复测上一轮”为主任务。
- 当前工作区上额外包含一小轮顺手推进：
  - `Session` 生命周期结构化事件：`session.started` / `session.closed` / `session.error`
  - `v2` 文档补充：`happy` 参考与“顺手重构 / 延期重构”清单
- 当前工作区状态：
  - 当前并非干净工作区，仍有未提交改动
  - 这些改动以文档更新和 `Session` 事件边界收口为主，适合直接在下个设备继续开发

## 0.1 当前任务状态

- 当前任务处于：`本轮已验证通过，准备切设备继续开发`
- 当前不需要再重复跑上一轮 `M8.1` 收口验证；
- 当前下一步应直接进入：
  - 继续推进 `M8.2 ListWorkspace`
  - 优先把监控与筛选面做成最小闭环

## 0.2 协作偏好

- 后续任务尽量按多 agent / 并行方式推进。
- 任务默认先实现、再由你手动验收，不把“本地已改完”视为完成。

## 1. 当前需关注改动

切设备后仍然优先关注下面这些文件：

- `source/server/lib/session-output-parser.mjs`
- `source/server/lib/terminal-session-manager.mjs`
- `source/server/server.mjs`
- `source/web/app.js`
- `docs/推进/v2/README.md`
- `docs/推进/v2/交接.md`
- `docs/推进/v2/顺手重构与延期重构清单.md`
- `docs/推进/v2/happy参考/README.md`

当前这批改动的核心目标是：

- 把 `waiting` / `resumed` 从内联启发式逻辑抽成轻量 `parsed events` 层；
- 通过 websocket 发送 `parsed_event` 给前端；
- 让任务卡在“绑定已有 Session”时，立即同步该 Session 的当前运行态；
- 拦住已关闭 Session 上的无效输入，减少错误日志噪音；
- 在现有 `parsed events` 基础上补一层轻量生命周期事件：
  - `session.started`
  - `session.closed`
  - `session.error`

## 2. 当前实情

当前代码已经做到：

- 服务端新增了 `session-output-parser` 雏形；
- 输出流会开始产出 `parsed_event`，至少覆盖：
  - `session.waiting`
  - `session.resumed`
- `Session` 生命周期事件已开始产出：
  - `session.started`
  - `session.closed`
  - `session.error`
- 前端状态日志会接收这类事件并写入“状态输出”面板；
- `TaskCard` 绑定已有 `Session` 时，服务端会按该 `Session` 当前状态立即回填任务运行态；
- 旧的退出日志重复项已经去掉，避免和 `session.closed` 双重刷屏。

当前这轮已完成最终确认：

- `waiting / resumed` 回传正常；
- 绑定已有 `Session` 后任务运行态即时同步正常；
- 已关闭 `Session` 上的输入守卫正常；
- `session.started / session.closed` 生命周期日志可正常消费；
- 本轮可以收口，不再需要以“先复测”为主任务。

## 3. 下个设备优先方向

切设备后不必先重复上一轮验证，优先继续 `M8.2 ListWorkspace`。

建议优先顺序：

1. 继续推进 `M8.2 ListWorkspace` 的监控能力。
2. 默认让 `ListWorkspace` 优先消费结构化 `Session` 事件，而不是继续直接读原始终端文本。
3. 如需再补状态面，优先围绕现有事件模型扩展，而不是重做主流程。
4. 如果新增监控字段，保持 `TaskCard.runtimeStatus` 继续以绑定 `Session` 的真相回流为准。

## 4. 状态日志入口

如果下个设备上要看应用层状态，不是看原始终端区，而是看首页的：

- 导航栏 `日志`
- 页面中的“状态输出”面板

这个面板主要记录：

- 连接 / 重连
- 任务绑定
- Session waiting / resumed / started / closed / error
- 任务运行失败 / 终端错误

原始终端输出仍然看 `Session Terminal` 区。

## 5. 当前判断

当前最合理的下一步已经不再是“继续验证上一轮收口”，而是直接进入 `M8.2 ListWorkspace`。

建议顺序：

1. 保持当前 `Session` 结构化事件边界不被打散
2. 继续做 `ListWorkspace` 的监控、筛选和提示能力
3. 再视推进情况决定是否扩更多 `Session` 事件或补查询层收口

## 6. 一句话交接

上一轮 `M8.1` 收口验证已经通过；当前工作区已额外补上 `session.started / session.closed / session.error` 这层轻量生命周期事件和相关文档。换设备后不必先重跑上一轮验证，直接继续推进 `M8.2 ListWorkspace`。