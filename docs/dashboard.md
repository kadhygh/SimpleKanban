# SimpleKanban Dashboard

最后更新：2026-03-07

## 1. 项目定位

SimpleKanban 是一个面向 Unity 本地开发流程的任务与执行管理器。

当前阶段不追求完整协作框架，而是优先打通：

- 本地项目绑定
- 网页终端会话
- CLI 交互与状态可见
- 可扩展执行器模型

## 2. 全局推进状态

| 模块 | 状态 | 说明 |
|---|---|---|
| 需求澄清 | 已完成 | 核心需求已从“外部 terminal 聚焦”转为“网页终端交互” |
| 总体方案 | 已完成 | 已形成 `prepare.md` 作为当前阶段的总设计文档 |
| 文档骨架 | 已完成 | 已建立 dashboard、推进目录、v1 清单和项目 README |
| 技术方向 | 已确定 | 目标方向仍是 `React + xterm.js + Node.js + WebSocket + node-pty` |
| M1 本地服务 | 已完成 | 已实现本地 Node 服务、静态页面和项目状态接口 |
| M1 项目选择 | 已完成 | 已实现项目选择、持久化保存和页面展示 |
| 网页终端内核 | 未开始 | 尚未接入 PTY / 会话管理 |
| 执行器模型 | 未开始 | 尚未实现 `ExecutorProfile` 和命令注入机制 |
| TaskCard 业务层 | 未开始 | 当前未进入业务层实现 |
| 无限画布 / 看板 | 未开始 | 明确不属于当前里程碑 |

## 3. 当前锁定决策

- v1 以 **网页托管终端** 为核心，而不是外部 terminal 窗口。
- v1 以 **单机 / 单项目 / 单活跃终端** 为最小目标。
- 后续一个 `TaskCard` 可以关联多个可执行入口，因此底层先抽象 `ExecutorProfile`。
- `docs/rescource` 中的 `vibekanban` review 可作为参考，但不是必须照搬的实现依据。
- 当前 `M1` 采用零依赖本地 Node 骨架快速落地，后续在 `M2` 再升级到目标技术栈。

## 4. 当前里程碑

### M1：本地服务 + 项目选择
状态：已完成

已完成内容：

- 本地服务启动
- 页面可连接
- 选择并保存当前项目路径
- 页面展示当前项目信息

### M2：网页终端最小闭环
状态：未开始

目标：

- 创建 PTY 终端会话
- 网页显示终端输出
- 网页向终端写入输入
- 支持终端 resize

### M3：会话状态与恢复
状态：未开始

目标：

- 页面刷新后恢复终端快照
- 状态可见
- 生命周期可追踪

### M4：执行器模型
状态：未开始

目标：

- 增加执行器注册表
- 支持参数辅助
- 将执行器命令注入终端

## 5. 文档地图

- 总准备文档：`prepare.md`
- v1 目录入口：`docs/推进/v1/README.md`
- v1 清单：`docs/推进/v1/清单.md`
- 参考资料：`docs/rescource/vibe-kanban-cli-analysis.md`
- 参考资料：`docs/rescource/vibekanban项目分析.md`

## 6. 下一步

建议直接进入 `M2` 的可执行拆解：

1. 引入 `xterm.js`。
2. 接入 `node-pty`。
3. 打通终端输入、输出和 resize。
4. 设计 `TerminalSession` 的最小状态模型。
